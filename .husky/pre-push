#!/bin/sh
echo "Running pre-push checks (lint & build)..."
cd web || exit 1

if command -v pnpm >/dev/null 2>&1; then
  pnpm run lint || exit 1
  pnpm run build || exit 1
else
  npm run lint || exit 1
  npm run build || exit 1
fi
